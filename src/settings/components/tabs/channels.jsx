import React from 'react';
import ItemList from '../item-list.jsx';

const Channels = () => {
    //TODO get redux shit going
    const channels = [];
    return (<ItemList items={ channels }/>);
};

export default Channels;
